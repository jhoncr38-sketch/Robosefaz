[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q
markers =
    integration: testes Playwright contra o SIAT simulado (sem internet)
filterwarnings =
    ignore::DeprecationWarning:starlette.*
    ignore:.*httpx2.*
